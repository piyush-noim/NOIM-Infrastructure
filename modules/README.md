# Terraform modules

Reusable modules belong in this directory. Each module should define its inputs in `variables.tf`, outputs in `outputs.tf`, and document its purpose and examples in a module-level `README.md`.
